import Documentation from "df/docs/layouts/documentation";
import Method from "../../components/method";
import { methods } from "../../_methods";

export const meta = {
  title: "Operations reference",
  __filename
};

export default ({ children }) => (
  <Documentation {...meta}>{children}</Documentation>
);

An `operation` defines a set of SQL commands that will be executed in order against your warehouse. They are useful when you need to run arbitrary queries.

For examples and usage, check out the [operations guide](/guides/operations).

<Method {...methods.ref} />

<Method {...methods.self} />
