config {
    type: "test",
    dataset: "df_integration_test.example_table"
}

input "sample_data" {
    select 'hi' as faked union all
    select 'ben' as faked union all
    select 'sup?' as faked
}

select 'hi' as faked union all
select 'ben' as faked union all
select 'sup?' as faked
