config {
    type: "test",
    dataset: "example_table2"
}

input "sample_data" {
    select 'hi' as faked union all
    select 'ben' as faked union all
    select 'sup?' as faked
}

select 'hi' as faked union all
select 'ben' as faked union all
select 'sup?' as faked
