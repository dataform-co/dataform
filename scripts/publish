#!/bin/bash
set -e

# Update to a new version.

npx lerna version

# Build the project with the new version.

./scripts/build

# Publish the new built packages.

(cd build && npx lerna publish from-package $@)
