#!/bin/bash
set -e

# Run tslint.
bazel build //:tslint && BAZEL_BINDIR=. ./bazel-bin/tslint_/tslint --project tsconfig.json

# Run integration tests
bazel test ... \
  --test_tag_filters=integration \
  --build_tests_only \
  --test_env=USE_CLOUD_BUILD_NETWORK=true
