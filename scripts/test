#!/bin/bash
set -e

./scripts/build

## Run install on the example projects using locally developed packages.

(cd build && npx lerna bootstrap)

## Run tests.

(cd build && npx mocha tests/**/*.js $@)
