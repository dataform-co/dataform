config {
  type: "view"
}

select ${when(core.ProjectConfig.vars.fooVar === "bar", "1", "2")} as val union all
select 2 as val union all
select ${when(core.ProjectConfig.warehouse === "postgres", "3", "2")} as val
