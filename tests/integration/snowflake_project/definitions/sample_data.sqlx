config {
  type: "view"
}

select ${when(dataform.projectConfig.variables.fooVar === "bar", "1", "2")} as val union all
select 2 as val union all
select ${when(dataform.projectConfig.warehouse === "snowflake", "3", "2")} as val
